const fs = require('fs');

module.exports = {
        async generateMeme() {
            fs.readdirSync("./src/image_generators/meme_generators").map((x) => {
                
            });
        }
}